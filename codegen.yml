overwrite: true
schema: "./src/schema.graphql"
documents: null
generates:
    ./src/types/graphql.ts:
        plugins:
            - "typescript"
            - "typescript-resolvers"
            - "typescript-mongodb"
            - "typescript-document-nodes"
