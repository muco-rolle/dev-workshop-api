import { model, Document, Model, Schema, models } from 'mongoose';

const {{name}}Schema = new Schema({}, { timestamps: true });

export interface {{ucfirst name}}Model extends Document {}

export const {{ name}}Model: Model<{{ucfirst name}}Model> =
    models.{{ name}}Model || model<{{ucfirst name}}Model>('{{ucfirst name}}',
        {{name}}Schema);