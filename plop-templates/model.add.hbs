import { model, Document, Model, Schema, models } from 'mongoose';

const {{name}}Schema = new Schema({}, { timestamps: true });

export interface I{{ucfirst name}} extends Document {}

export const {{ucfirst name}}: Model<I{{ucfirst name}}> =
    models.{{ucfirst name}} || model<I{{ucfirst name}}>('{{ucfirst name}}',
        {{name}}Schema);